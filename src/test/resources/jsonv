#!/bin/sh

set -e

# Startup script for the LAPPS JSON schema compiler. Change the jsonc installation directory
# below if needed.
#
MEMSIZE=4G
VERSION=__VERSION__
JAR=$HOME/bin/jsonv-$VERSION.jar

java -jar $JAR $@
